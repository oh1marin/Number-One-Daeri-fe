# 카카오맵 (오시는길 페이지) - JavaScript 키
# https://developers.kakao.com/console/app → 앱 키 → JavaScript 키
# 플랫폼에 Web 추가 후 localhost 및 배포 도메인 등록 필요
NEXT_PUBLIC_KAKAO_MAP_KEY=

# API — 공지 목록 등. 반드시 http(s) 로 시작하는 절대 URL (예: https://api.example.com/api/v1).
# web-home은 관리자와 달리 /api/v1 만 넣는 상대 경로를 쓰면 안 됨(Next가 자기 호스트로 요청 → 실패 → 샘플 공지).
# 백엔드: GET /api/v1/notices 와 GET /notices(루트) 둘 다 지원.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5174/api/v1
